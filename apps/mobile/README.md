# EvalCap Mobile

React Native app — coming after web MVP is validated.

## Planned Stack
- React Native + Expo
- Shared API calls with `packages/shared`
- Same Supabase backend as web app
- Same Claude API backend (via shared Next.js API or separate serverless functions)

## Notes
- Do not start building until web MVP is live and validated
- Shared business logic will live in `packages/shared`
